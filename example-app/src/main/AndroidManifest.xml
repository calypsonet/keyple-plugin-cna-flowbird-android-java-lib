<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.calypsonet.keyple.plugin.flowbird.example">

    <uses-permission android:name="android.permission.NFC" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="com.parkeon.data.dallas.READ_CONTENT" />
    <uses-permission android:name="com.parkeon.data.dallas.WRITE_CONTENT" />

    <uses-feature
        android:name="android.hardware.nfc"
        android:required="true"/>

    <application
        android:name="org.calypsonet.keyple.plugin.flowbird.example.DemoApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name="org.calypsonet.keyple.plugin.flowbird.example.activity.MainActivity"
            android:clearTaskOnLaunch="true"
            android:label="Keyple Flowbird demo"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
